# backend/.env

# ----------------------------------------------------
# 1. MONGODB CONNECTION
# ----------------------------------------------------
# URI for local MongoDB running inside the Docker container named 'mongodb_expense'.
# It uses the credentials (mongouser:mongopass) defined in the 'docker run' command.
MONGO_URI=mongodb://mongouser:mongopass@localhost:27017/expense_db?authSource=admin

# ----------------------------------------------------
# 2. SERVER CONFIG
# ----------------------------------------------------
PORT=5000

# ----------------------------------------------------
# 3. JSON WEB TOKEN (JWT) SECRET
# ----------------------------------------------------
# This secret is used to sign and verify your JWTs. 
# !!! Replace the value below with a long, random, and complex string !!!
JWT_SECRET=THIS_IS_A_DEV_SECRET_REPLACE_ME_IN_PROD_001A79B3